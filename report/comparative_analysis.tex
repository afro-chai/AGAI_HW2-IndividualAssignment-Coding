% Comparative analysis report (CO2 StockTrader)
% Compile: pdflatex comparative_analysis
% Submit as report/report.pdf per course instructions (rename PDF after build).
\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\usepackage[hidelinks]{hyperref}

\newcommand{\StudentName}{Your Name}
\newcommand{\Course}{AGAI --- Individual Assignment 2 (Coding)}
\newcommand{\Repo}{\texttt{AGAI\_HW2-IndividualAssignment-Coding}}

\title{Building StockTrader:\\Signals, Strategies, and Disagreement}
\author{\StudentName}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This report compares two graded behavioral strategies---\textbf{News Sentiment Follower} and \textbf{Volatility Averse}---plus an extension \textbf{Moral Trader} agent, implemented with Microsoft AutoGen and local inference (Ollama). All agents consume the same structured market JSON per ticker. The analysis reflects outputs committed in \Repo{} for tickers NVDA, TSLA, JNJ, and KO (run date in JSON: 2026-04-08).
\end{abstract}

\section{Strategy Selection and Rationale}

\textbf{Graded pair.} I selected \textbf{News Sentiment Follower} and \textbf{Volatility Averse} because they embody a clean philosophical contrast: the first prioritizes \emph{external narrative and sentiment} when available, while the second prioritizes \emph{internal stability and tail risk} from price paths (volatility, drawdowns, ATR).

\textbf{Why this pairing.} I expected disagreement when headline sentiment is strong but price action is choppy (news-driven optimism vs.\ risk-off posture), and agreement when both signals are ambiguous (e.g., quiet tape, middling volatility).

\textbf{Extension.} I added \textbf{Moral Trader} as a third parallel branch (bonus/capstone-oriented lens on narrative opacity and accountability), evaluated alongside the core pair.

\section{System Architecture}

\subsection{Orchestration logic}
\begin{itemize}
  \item \textbf{Market data (no LLM):} \texttt{src/market\_data.py} pulls yfinance history and derived features; optionally calls Alpha Vantage for news sentiment.
  \item \textbf{Strategy agents (LLM):} three \texttt{AssistantAgent} instances run in parallel per ticker with structured outputs (decision, confidence, justification).
  \item \textbf{Isolation:} strategies receive the same JSON context and do not see each other's outputs before the evaluator.
  \item \textbf{Evaluator (LLM):} compares outputs and writes consensus vs.\ divergence analysis.
  \item \textbf{Persistence:} per-ticker JSON under \texttt{outputs/}, plus \texttt{summary.json} and optional \texttt{backtest.json}.
\end{itemize}

\subsection{Diagram}
\begin{center}
\begin{tikzpicture}[
  node distance=8mm and 12mm,
  box/.style={draw, rounded corners, align=center, minimum width=3.2cm, minimum height=0.9cm},
  arr/.style={-{Stealth[length=2mm]}}
]
\node[box] (md) {MarketData\\(yfinance + features)};
\node[box, below left=of md] (a) {Strategy A\\News};
\node[box, below=of md] (b) {Strategy B\\Volatility};
\node[box, below right=of md] (c) {Strategy C\\Moral};
\node[box, below=3.2cm of md] (ev) {Evaluator};
\draw[arr] (md) -- (a);
\draw[arr] (md) -- (b);
\draw[arr] (md) -- (c);
\draw[arr] (a) -- (ev);
\draw[arr] (b) -- (ev);
\draw[arr] (c) -- (ev);
\end{tikzpicture}
\end{center}

\section{Stock Selection and Rationale}

Table~\ref{tab:stocks} lists four U.S.\ equities chosen to span different regimes: large-cap growth/momentum proxy (NVDA), high-beta volatile name (TSLA), defensive healthcare (JNJ), and stable consumer staple (KO).

\begin{table}[h]
  \centering
  \begin{tabular}{@{}ll@{}}
    \toprule
    Ticker & Intended regime \\
    \midrule
    NVDA & Large-cap, high attention, tech beta \\
    TSLA & Volatile / momentum-sensitive \\
    JNJ & Defensive, lower drama \\
    KO & Staple, typically lower realized vol \\
    \bottomrule
  \end{tabular}
  \caption{Stock selection intent (qualitative).}
  \label{tab:stocks}
\end{table}

\section{Results by Stock}

Table~\ref{tab:results} summarizes graded strategies A/B and extension C from \texttt{outputs/summary.json}. Confidence values come from each ticker file.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    Ticker & A (News) & Conf. & B (Vol.) & Conf. \\
    \midrule
    NVDA & HOLD & 8 & HOLD & 8 \\
    TSLA & HOLD & 8 & HOLD & 8 \\
    JNJ  & HOLD & 8 & HOLD & 8 \\
    KO   & HOLD & 8 & HOLD & 8 \\
    \bottomrule
  \end{tabular}
  \caption{Decisions for the two graded strategies (extension Moral Trader also HOLD throughout this run).}
  \label{tab:results}
\end{table}

\subsection{Direct excerpts from JSON outputs}

\textbf{NVDA (News Sentiment Follower):} \emph{``No recent news available due to lack of API key. Current price (\$181.885) is near the moving average (177.2422), indicating a neutral trend. Volatility remains low (0.02413).''}

\textbf{TSLA (Volatility Averse):} \emph{``Low volatility\_30d (0.024147) and std\_daily\_return\_90d (0.023333) indicate stable price path. Max\_single\_day\_drop\_90d (-0.054215) is within acceptable bounds.''}

\subsection{Evaluator}
Across tickers, the evaluator generally noted unanimous HOLD with different emphasis (news availability vs.\ volatility metrics vs.\ narrative gaps). Example (TSLA): \emph{``All strategy agents converge on the same decision for this ticker. They differ slightly in rationale emphasis, but the final recommendation is aligned.''}

\section{Patterns of Agreement and Disagreement}

\textbf{Observed pattern in this run: broad agreement.} For all four tickers, strategies A/B/C agreed on \textbf{HOLD}, yielding \texttt{total\_agreements = 4} and \texttt{total\_disagreements = 0} in \texttt{outputs/summary.json}.

\textbf{Interpretation (not just restatement).} Convergence is plausible given:
\begin{itemize}
  \item \textbf{Missing news channel:} \texttt{news\_features} shows \texttt{articles\_count = 0} and \texttt{note: "no API key"}, so the News agent cannot exploit sentiment overrides and defaults to conservative HOLD language tied to price/vol metrics.
  \item \textbf{Similar intermediate regimes:} several names show moderate realized volatility and non-extreme drawdowns on the snapshot, which the Volatility Averse agent often maps to HOLD under its thresholds.
\end{itemize}

\textbf{Where sharper disagreement would likely appear.} With an Alpha Vantage key (and respecting rate limits), strong positive/negative \texttt{avg\_sentiment\_score} could push Strategy A toward BUY/SELL while Strategy B remains risk-focused---especially on high-beta names.

\section{Failure or Surprise Case}

\textbf{Surprise:} uniform HOLD across a deliberately diverse ticker set limits the pedagogical contrast the assignment encourages.

\textbf{Likely causes:} (1) absent news API key, (2) local model tendency toward conservative decisions on structured financial JSON, (3) evaluator normalization when unanimity holds.

\textbf{Mitigation for future runs:} add \texttt{ALPHAVANTAGE\_API\_KEY}, consider tickers with more extreme recent moves, and review prompts if the model over-uses HOLD.

\section{Reflection}

If I had to follow \textbf{one graded strategy for one month with \$10{,}000}, I would choose \textbf{Volatility Averse} on this implementation because, without reliable headline sentiment, the News agent is partially blind; the volatility-focused agent at least anchors on measurable path risk.

A \textbf{hybrid} would gate sentiment: only allow Strategy A to override when $\lvert\texttt{avg\_sentiment\_score}\rvert$ exceeds a threshold \emph{and} news volume is non-trivial; otherwise defer to Strategy B's risk checks.

\appendix
\section*{Bonus artifact: backtest snapshot}
The repository includes \texttt{outputs/backtest.json} (heuristic, not identical to live LLM decisions). Overall winner in that file: \textbf{volatility\_averse\_heuristic} (see \texttt{overall\_winner}). Per-ticker hit rates vary; treat this as a transparent scoring artifact per the bonus specification.

\end{document}
