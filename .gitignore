.venv/
__pycache__/
*.pyc
.env
# Optional raw API key file (first line = key only)
secrets/alphavantage_api_key.txt
# May show API key in a screenshot — keep local only
docs/assets/alphavantage-welcome.png
*.egg-info/
.pytest_cache/

# Local assignment brief / notes (not part of submission tree)
Instructions/

# Run transcripts live under logs/ — commit sanitized copies only (never paste API keys into logs).
