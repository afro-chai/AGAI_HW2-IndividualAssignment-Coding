# src/

Python package: CLI (`main.py`), market data and Alpha Vantage integration (`market_data.py`), AutoGen orchestration (`orchestration.py`), strategy helpers (`strategies.py`), evaluator (`evaluator.py`), schemas (`schemas.py`), LLM client factory (`llm_factory.py`), historical backtest (`backtest.py`).
