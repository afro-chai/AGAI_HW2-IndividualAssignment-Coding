# outputs/

Committed JSON artifacts from pipeline runs: one file per ticker (`<SYMBOL>.json`), `summary.json`, and optional `backtest.json` when using `--backtest`. Included so graders can inspect outputs without API keys; regenerate locally after changing prompts or tickers.
